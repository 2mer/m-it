{
	"name": "@sgty/m-it",
	"description": "minimal event emitter",
	"version": "1.0.2",
	"main": "dist/index.js",
	"private": false,
	"scripts": {
		"test": "jest",
		"test-watch": "jest --watchAll",
		"build": "rm -rf ./dist/ && tsc",
		"prepare": "yarn build"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@types/jest": "^28.1.4",
		"jest": "^28.1.2",
		"ts-jest": "^28.0.5",
		"typescript": "^4.7.4"
	}
}
